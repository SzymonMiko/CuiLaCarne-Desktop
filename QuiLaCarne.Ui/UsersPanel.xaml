<Page
    x:Class="QuiLaCarne.Ui.UsersPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="#F5F5F5">

    <Page.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#19A93A"/>
            <Setter Property="Padding" Value="18 8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Background="#19A93A">
            <TextBlock Text="Qui la Carne"
                       Foreground="White"
                       FontSize="22"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="24 0"/>

            <TextBlock Text="Users Panel"
                       DockPanel.Dock="Right"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="0 0 24 0"/>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid Margin="24" MinWidth="760">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock FontSize="46"
                           FontWeight="Bold">
                    <Run Text="Users " Foreground="#E53935"/>
                    <Run Text="Management" Foreground="#1B9A2F"/>
                </TextBlock>

                <Grid Margin="0 8 0 18"
                      Height="6"
                      MaxWidth="360"
                      HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <Rectangle Fill="#19A93A" RadiusX="3" RadiusY="3"/>
                    <Rectangle Grid.Column="1" Fill="#E0E0E0" RadiusX="3" RadiusY="3"/>
                    <Rectangle Grid.Column="2" Fill="#E53935" RadiusX="3" RadiusY="3"/>
                </Grid>

                <TextBlock Text="Lista użytkowników zsynchronizowana z lokalnej bazy SQLite"
                           Foreground="#333333"
                           FontSize="16"/>
            </StackPanel>

            <Border Grid.Row="2"
                    Background="White"
                    CornerRadius="22"
                    Padding="18">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16"
                                      ShadowDepth="3"
                                      Opacity="0.18"/>
                </Border.Effect>

                <DataGrid ItemsSource="{Binding Users}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          RowHeight="42"
                          FontSize="14">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Username"
                                            Binding="{Binding Username}"
                                            Width="*"/>

                        <DataGridTextColumn Header="Email"
                                            Binding="{Binding Email}"
                                            Width="2*"/>

                        <DataGridCheckBoxColumn Header="Enabled"
                                                Binding="{Binding IsEnabled}"
                                                Width="Auto"/>

                        <DataGridTextColumn Header="Token"
                                            Binding="{Binding Token}"
                                            Width="2*"/>

                        <DataGridTextColumn Header="Updated"
                                            Binding="{Binding UpdatedAt}"
                                            Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
        </ScrollViewer>
    </Grid>
</Page>
