<Page x:Class="QuiLaCarne.Ui.PersonnelManagement"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="PersonnelManagement"
      Background="#F5F5F5">

    <Page.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#19A93A"/>
            <Setter Property="Padding" Value="18 8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Background="#19A93A">
            <TextBlock Text="Qui la Carne" Foreground="White" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" Margin="24 0"/>
            <TextBlock Text="Personnel Management" DockPanel.Dock="Right" Foreground="White" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0 0 24 0"/>
        </DockPanel>

        <Grid Grid.Row="1" Margin="36">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="22" Padding="18">
                <Border.Effect><DropShadowEffect BlurRadius="16" ShadowDepth="3" Opacity="0.18"/></Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Margin="0 0 0 16">
                        <TextBlock FontSize="40" FontWeight="Bold"><Run Text="Staff " Foreground="#E53935"/><Run Text="accounts" Foreground="#1B9A2F"/></TextBlock>
                        <TextBlock Text="Add employees, assign roles and block accounts." Foreground="#333333" FontSize="15"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" AutoGenerateColumns="False" IsReadOnly="True" RowHeight="42" FontSize="14" GridLinesVisibility="None">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"/>
                            <DataGridTextColumn Header="Roles" Binding="{Binding RolesText}" Width="*"/>
                            <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsEnabled}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="22" Padding="22">
                <Border.Effect><DropShadowEffect BlurRadius="16" ShadowDepth="3" Opacity="0.18"/></Border.Effect>
                <StackPanel>
                    <TextBlock Text="Employee form" FontSize="26" FontWeight="Bold" Margin="0 0 0 14"/>
                    <TextBlock Text="Username" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" Height="36" Margin="0 4 0 10"/>
                    <TextBlock Text="Email" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding NewEmail, UpdateSourceTrigger=PropertyChanged}" Height="36" Margin="0 4 0 10"/>
                    <TextBlock Text="Password" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" Height="36" Margin="0 4 0 10"/>
                    <TextBlock Text="Role" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding AvailableRoles}" SelectedItem="{Binding SelectedRole}" Height="36" Margin="0 4 0 18"/>
                    <Button Content="Add employee" Command="{Binding AddEmployeeCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="42"/>
                    <Button Content="Change selected role" Command="{Binding ChangeRoleCommand}" Style="{StaticResource PrimaryButtonStyle}" Background="#19A93A" Height="42" Margin="0 10 0 0"/>
                    <Button Content="Block / unblock account" Command="{Binding ToggleBlockCommand}" Style="{StaticResource PrimaryButtonStyle}" Background="#E53935" Height="42" Margin="0 10 0 0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
